import { Router } from 'express';

const router = Router();

// Placeholder routes
router.post('/heir-access', (req, res) => {
  res.status(501).json({ message: 'Emergency routes not implemented yet' });
});

export default router;
